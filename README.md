# Amazing sort

For every use case there's an npm package. So why not to use _all_ of them?

## Install

```
yarn add amazing-sort
```

## Usage

If you managed to pass the install phase, run:

```node
const sort = require('amazing-sort')
const array = [1, 3, 2]

sort(array) // 1, 2, 3
```
